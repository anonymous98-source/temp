Perfect 👍
Below is a microservice-ready Email Service with HTML email + attachment support, designed using clean architecture, SOLID principles, and production best practices.

This is suitable for:

Independent deployment

Integration with other microservices

Async / message-driven extension later (Kafka/RabbitMQ)



---

🧩 Microservice Architecture – Email Service

🔹 Key Design Decisions

Stateless service

DTO → Service → Infrastructure separation

HTML emails using MimeMessage

Multiple attachments support

Exception abstraction

Config externalized



---

1️⃣ Folder Structure (Microservice Ready)

email-service
│
├── src/main/java/com/example/email
│   ├── EmailServiceApplication.java
│
│   ├── api
│   │   └── EmailController.java
│
│   ├── application
│   │   └── EmailApplicationService.java
│
│   ├── domain
│   │   └── EmailMessage.java
│
│   ├── infrastructure
│   │   └── mail
│   │       └── SmtpEmailSender.java
│
│   ├── ports
│   │   └── EmailSenderPort.java
│
│   └── exception
│       ├── EmailSendException.java
│       └── GlobalExceptionHandler.java
│
├── src/main/resources
│   ├── templates
│   │   └── sample-email.html
│   └── application.yml
│
└── pom.xml

> This follows Hexagonal / Clean Architecture




---

2️⃣ pom.xml Dependencies

<dependencies>

    <!-- Web -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Mail -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>

    <!-- Validation -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Thymeleaf for HTML Emails -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>

</dependencies>


---

3️⃣ application.yml

server:
  port: 8082

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true


---

4️⃣ Domain Model

EmailMessage.java

package com.example.email.domain;

import java.io.File;
import java.util.List;

public class EmailMessage {

    private String to;
    private String subject;
    private String htmlBody;
    private List<File> attachments;

    public EmailMessage(String to, String subject, String htmlBody, List<File> attachments) {
        this.to = to;
        this.subject = subject;
        this.htmlBody = htmlBody;
        this.attachments = attachments;
    }

    public String getTo() { return to; }
    public String getSubject() { return subject; }
    public String getHtmlBody() { return htmlBody; }
    public List<File> getAttachments() { return attachments; }
}


---

5️⃣ Port (Interface)

EmailSenderPort.java

package com.example.email.ports;

import com.example.email.domain.EmailMessage;

public interface EmailSenderPort {
    void send(EmailMessage emailMessage);
}


---

6️⃣ Infrastructure – SMTP Implementation

SmtpEmailSender.java

package com.example.email.infrastructure.mail;

import com.example.email.domain.EmailMessage;
import com.example.email.ports.EmailSenderPort;
import jakarta.mail.MessagingException;
import jakarta.mail.internet.MimeMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.mail.javamail.MimeMessageHelper;
import org.springframework.stereotype.Component;

import java.io.File;

@Component
public class SmtpEmailSender implements EmailSenderPort {

    private final JavaMailSender mailSender;

    public SmtpEmailSender(JavaMailSender mailSender) {
        this.mailSender = mailSender;
    }

    @Override
    public void send(EmailMessage emailMessage) {
        try {
            MimeMessage mimeMessage = mailSender.createMimeMessage();
            MimeMessageHelper helper =
                    new MimeMessageHelper(mimeMessage, true, "UTF-8");

            helper.setTo(emailMessage.getTo());
            helper.setSubject(emailMessage.getSubject());
            helper.setText(emailMessage.getHtmlBody(), true);

            if (emailMessage.getAttachments() != null) {
                for (File file : emailMessage.getAttachments()) {
                    helper.addAttachment(file.getName(), file);
                }
            }

            mailSender.send(mimeMessage);

        } catch (MessagingException e) {
            throw new RuntimeException("Failed to send email", e);
        }
    }
}


---

7️⃣ Application Service (Business Logic)

EmailApplicationService.java

package com.example.email.application;

import com.example.email.domain.EmailMessage;
import com.example.email.ports.EmailSenderPort;
import org.springframework.stereotype.Service;

import java.io.File;
import java.util.List;

@Service
public class EmailApplicationService {

    private final EmailSenderPort emailSender;

    public EmailApplicationService(EmailSenderPort emailSender) {
        this.emailSender = emailSender;
    }

    public void sendHtmlEmail(String to, String subject, String html, List<File> attachments) {
        EmailMessage email = new EmailMessage(to, subject, html, attachments);
        emailSender.send(email);
    }
}


---

8️⃣ REST Controller (Multipart Support)

EmailController.java

package com.example.email.api;

import com.example.email.application.EmailApplicationService;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

@RestController
@RequestMapping("/api/v1/email")
public class EmailController {

    private final EmailApplicationService emailService;

    public EmailController(EmailApplicationService emailService) {
        this.emailService = emailService;
    }

    @PostMapping(value = "/send", consumes = "multipart/form-data")
    public ResponseEntity<String> sendEmail(
            @RequestParam String to,
            @RequestParam String subject,
            @RequestParam String htmlBody,
            @RequestParam(required = false) MultipartFile[] attachments
    ) throws Exception {

        List<File> files = new ArrayList<>();
        if (attachments != null) {
            for (MultipartFile mf : attachments) {
                File file = File.createTempFile("attach-", mf.getOriginalFilename());
                mf.transferTo(file);
                files.add(file);
            }
        }

        emailService.sendHtmlEmail(to, subject, htmlBody, files);
        return ResponseEntity.ok("Email sent successfully");
    }
}


---

9️⃣ HTML Email Template (Optional)

resources/templates/sample-email.html

<!DOCTYPE html>
<html>
<body>
<h2>Hello 👋</h2>
<p>This is a <b>HTML email</b> sent from a Spring Boot microservice.</p>
<p style="color:gray;">– Email Service</p>
</body>
</html>


---

🔐 Microservice Ready Features

✔ Stateless
✔ Independent deployable
✔ SMTP replaceable
✔ Kafka/RabbitMQ friendly
✔ HTML + attachments
✔ Clean architecture
✔ Testable


---

🚀 Next Level (Highly Recommended)

Since you’re working with microservices & Spring Boot, I can:

Add Kafka-based async email queue

Add Retry + DLQ

Add JWT-secured endpoint

Dockerize + Kubernetes ready

Add Email template rendering via Thymeleaf


Just tell me what you want next 👌